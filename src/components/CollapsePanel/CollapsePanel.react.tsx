import React from "react";
import CollapsePanelView from "./CollapsePanelView.react";

const CollapsePanel: React.FC<any> = props => {
  return <CollapsePanelView {...props} />
};

export default CollapsePanel;
